<footer class="phpeitor-footer">
  <div class="phpeitor-footer__inner">
    <div class="phpeitor-footer__brand">
      <span>© 2026 Docs Astro - A project by Metadatape. Designed by PHPeitor.</span>
    </div>
    <nav aria-label="Enlaces del sitio" class="phpeitor-footer__links">
      <a href="/docs/">Documentación</a>
      <a href="/docs/#modelo">Dataset</a>
      <a href="/">Visor BI</a>
      <a href="http://161.132.4.164:8090/login/" target="_blank" rel="noreferrer">PHPeitor</a>
    </nav>
  </div>
  <div class="phpeitor-footer__mark" aria-hidden="true">
    <svg viewBox="0 0 1500 300" preserveAspectRatio="xMidYMid meet" role="presentation">
      <text x="750" y="220" text-anchor="middle" class="phpeitor-word">PHPEITOR</text>
      <text x="750" y="220" text-anchor="middle" class="phpeitor-trace">PHPEITOR</text>
    </svg>
  </div>
</footer>

<script is:inline>
  const footer = document.querySelector('.phpeitor-footer');
  if (footer && !footer.dataset.pageFooter) {
    footer.dataset.pageFooter = 'true';
    document.body.appendChild(footer);
  }
</script>

<style>
  .phpeitor-footer {
    box-sizing: border-box;
    width: 100% !important;
    max-width: none !important;
    position: relative;
    z-index: 100;
    left: 0 !important;
    margin: 5rem 0 0 !important;
    margin-left: 0 !important;
    padding: 2rem 3.5rem .5rem;
    color: #20233f;
    background: #f1ebdf;
    overflow: hidden;
  }
  .phpeitor-footer::before {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    height: 2px;
    background: linear-gradient(90deg, #f43f5e 0 25%, #4052e8 25% 75%, #f43f5e 75%);
    content: '';
  }
  .phpeitor-footer__inner,
  .phpeitor-footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    max-width: none;
    width: 100%;
    margin: 0 auto;
  }
  .phpeitor-footer__brand strong,
  .phpeitor-footer__brand span { display: block; }
  .phpeitor-footer__brand strong { color: #111536; font-size: 1.1rem; }
  .phpeitor-footer__brand span,
  .phpeitor-footer__bottom { color: #65657a; font-size: .8rem; }
  .phpeitor-footer__links { display: flex; flex-wrap: wrap; gap: 1.25rem; }
  .phpeitor-footer__links a { color: #20233f; font-size: .85rem; text-decoration: none; }
  .phpeitor-footer__links a:hover { color: #f43f5e; }
  .phpeitor-footer__bottom { padding-top: 2rem; margin-top: 2rem; border-top: 1px solid #d5cdbc; }
  .phpeitor-footer__mark {
    position: relative;
    display: grid;
    place-items: center;
    margin: 1.5rem -3.5rem 0;
    width: calc(100% + 7rem);
    height: clamp(14rem, 24vw, 28rem);
    overflow: hidden;
    user-select: none;
  }
  .phpeitor-footer__mark::before {
    content: none;
  }
  .phpeitor-footer__mark svg { width: 106%; height: 100%; max-width: none; overflow: visible; }
  .phpeitor-word,
  .phpeitor-trace {
    font-family: Arial Black, Arial, sans-serif;
    font-size: 230px;
    font-style: italic;
    font-weight: 900;
    letter-spacing: -14px;
  }
  .phpeitor-word { fill: #060c3b; stroke: #060c3b; stroke-width: 3; }
  .phpeitor-trace {
    fill: none;
    stroke: #f43f5e;
    stroke-width: 3;
    stroke-dasharray: 42 260;
    stroke-linecap: round;
    filter: drop-shadow(0 0 5px #f43f5e);
    animation: phpeitor-letter-trace 5s linear infinite;
  }
  @keyframes phpeitor-letter-trace {
    to { stroke-dashoffset: -302px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .phpeitor-trace { animation: none; }
  }
  @media (max-width: 640px) {
    .phpeitor-footer { margin-left: 0; padding-inline: 1.5rem; }
    .phpeitor-footer__mark { margin-inline: -1.5rem; width: calc(100% + 3rem); }
    .phpeitor-footer__inner { align-items: flex-start; flex-direction: column; }
    .phpeitor-footer__bottom { align-items: flex-start; flex-direction: column; gap: .5rem; }
  }
</style>
