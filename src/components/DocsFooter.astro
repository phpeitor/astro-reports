<footer class="phpeitor-footer">
  <div class="phpeitor-footer__inner">
    <div class="phpeitor-footer__brand">
      <span>© 2026 Docs Astro - A project by Metadatape. Designed by PHPeitor.</span>
    </div>
    <nav aria-label="Enlaces del sitio" class="phpeitor-footer__links">
      <a href="/docs/">Documentación</a>
      <a href="/docs/#modelo">Dataset</a>
      <a href="/">Visor BI</a>
      <a href="http://161.132.4.164:8090/login/" target="_blank" rel="noreferrer">PHPeitor</a>
    </nav>
  </div>
  <div class="phpeitor-footer__mark" aria-hidden="true"><span>PHPEITOR</span></div>
</footer>

<script is:inline>
  const footer = document.querySelector('.phpeitor-footer');
  if (footer && !footer.dataset.pageFooter) {
    footer.dataset.pageFooter = 'true';
    document.body.appendChild(footer);
  }
</script>

<style>
  .phpeitor-footer {
    width: 100% !important;
    max-width: none !important;
    position: relative;
    z-index: 100;
    left: 0 !important;
    margin: 5rem 0 0 !important;
    margin-left: 0 !important;
    padding: 3.5rem 3.5rem 1.25rem;
    color: #20233f;
    background: #f1ebdf;
    overflow: hidden;
  }
  .phpeitor-footer::before {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    height: 2px;
    background: linear-gradient(90deg, #f43f5e 0 25%, #4052e8 25% 75%, #f43f5e 75%);
    content: '';
  }
  .phpeitor-footer__inner,
  .phpeitor-footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    max-width: none;
    width: 100%;
    margin: 0 auto;
  }
  .phpeitor-footer__brand strong,
  .phpeitor-footer__brand span { display: block; }
  .phpeitor-footer__brand strong { color: #111536; font-size: 1.1rem; }
  .phpeitor-footer__brand span,
  .phpeitor-footer__bottom { color: #65657a; font-size: .8rem; }
  .phpeitor-footer__links { display: flex; flex-wrap: wrap; gap: 1.25rem; }
  .phpeitor-footer__links a { color: #20233f; font-size: .85rem; text-decoration: none; }
  .phpeitor-footer__links a:hover { color: #f43f5e; }
  .phpeitor-footer__bottom { padding-top: 2rem; margin-top: 2rem; border-top: 1px solid #d5cdbc; }
  .phpeitor-footer__mark {
    position: relative;
    display: grid;
    place-items: center;
    margin-top: 4rem;
    width: 100%;
    height: clamp(11rem, 30vw, 24rem);
    overflow: hidden;
    user-select: none;
  }
  .phpeitor-footer__mark::before {
    position: absolute;
    inset: 18% 1% 12%;
    border: 2px solid transparent;
    border-top-color: #f43f5e;
    border-right-color: #4052e8;
    border-bottom-color: #f43f5e;
    border-radius: 50%;
    content: '';
    animation: phpeitor-footer-snake 4s linear infinite;
  }
  .phpeitor-footer__mark span {
    position: relative;
    z-index: 1;
    color: #060c3b;
    width: 100%;
    font-size: clamp(5rem, 19vw, 19rem);
    font-style: italic;
    font-weight: 900;
    letter-spacing: -.12em;
    line-height: .7;
    white-space: nowrap;
    text-align: center;
    -webkit-text-stroke: 2px #f43f5e;
    text-shadow: 0 0 4px #f43f5e, 0 0 18px rgba(244, 63, 94, .45);
    transform: skewX(-10deg);
    animation: phpeitor-footer-glow 4s ease-in-out infinite alternate;
  }
  @keyframes phpeitor-footer-snake {
    from { transform: rotate(-2deg) scaleX(1); }
    to { transform: rotate(2deg) scaleX(1.04); }
  }
  @keyframes phpeitor-footer-glow {
    from { -webkit-text-stroke-color: #f43f5e; text-shadow: 0 0 3px #f43f5e, 0 0 12px rgba(244, 63, 94, .35); }
    to { -webkit-text-stroke-color: #4052e8; text-shadow: 0 0 8px #f43f5e, 0 0 28px rgba(64, 82, 232, .5); }
  }
  @media (prefers-reduced-motion: reduce) {
    .phpeitor-footer__mark::before { animation: none; }
  }
  @media (max-width: 640px) {
    .phpeitor-footer { margin-left: 0; padding-inline: 1.5rem; }
    .phpeitor-footer__inner { align-items: flex-start; flex-direction: column; }
    .phpeitor-footer__bottom { align-items: flex-start; flex-direction: column; gap: .5rem; }
  }
</style>
