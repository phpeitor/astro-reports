<section class="panel card-panel" aria-labelledby="form-title">
  <div class="panel-head">
    <span class="panel-icon" aria-hidden="true">+</span>
    <div>
      <p class="panel-kicker">Administración</p>
      <h2 id="form-title">Nuevo reporte</h2>
      <p>Conecta una visualización a tu workspace.</p>
    </div>
  </div>

  <form id="report-form" class="report-form" novalidate>
    <label class="field">
      <span>Nombre del reporte</span>
      <input id="report-name" name="name" type="text" placeholder="Ej: KPI de Rentabilidad" required />
    </label>

    <label class="field">
      <span>Categoria o modulo</span>
      <select id="report-module" name="module" required>
        <option value="">Selecciona un modulo</option>
        <option value="Operaciones">Operaciones</option>
        <option value="Finanzas">Finanzas</option>
        <option value="Comercial">Comercial</option>
      </select>
    </label>

    <label class="field">
      <span>URL del reporte</span>
      <input
        id="report-url"
        name="url"
        type="url"
        placeholder="https://app.fabric.microsoft.com/view?r=..."
        required
      />
    </label>

    <label class="field">
      <span>Plataforma</span>
      <select id="report-platform" name="platform">
        <option value="auto">Deteccion automatica</option>
        <option value="powerbi">Power BI</option>
        <option value="tableau">Tableau</option>
        <option value="superset">Apache Superset</option>
        <option value="qlik">Qlik Sense</option>
        <option value="grafana">Grafana</option>
        <option value="looker">Looker Studio</option>
        <option value="other">Otra plataforma</option>
      </select>
    </label>

    <button class="primary-btn" type="submit">GUARDAR <span aria-hidden="true">→</span></button>
    <p class="form-message" id="form-message" role="status" aria-live="polite"></p>
  </form>
</section>
